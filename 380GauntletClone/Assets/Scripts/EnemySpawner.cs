using System.Collections;
using System.Collections.Generic;
using System.Xml;
using UnityEngine;

public class EnemySpawner : MonoBehaviour
{
    /*
    public GameObject prefabTest;
    public GameObject prefabTestLVL2;
    public GameObject prefabTestLVL3;
    //public GameObject prefab2Test;

    [SerializeField]
    private int spLevel;

    [SerializeField]
    private bool spLevel1, spLevel2, spLevel3;

    [SerializeField]
    private bool isBone, isBlock;

    private int spawnerHealth;

    [SerializeField]
    private bool playerEntered;

    [SerializeField]
    private bool isKilled;

    private void Update()
    {
        if (playerEntered)
        {
            if (isBlock)
            {
                for (int i = 0; i < 10; i++)
                {
                    spawnBlock(spLevel);
                    isBlock = false;
                }
            }

        }

        if (isKilled)
        {
            DestroySpawner();
        }
    }


    public void spawnBlock(int eLevel)
    {
        if (spLevel1)
        {
            Instantiate(prefabTest);
        } else if (spLevel2){
            Instantiate(prefabTestLVL2);
        } else if (spLevel3)
        {
            Instantiate(prefabTestLVL3);
        }


    }

    public void DestroySpawner()
    {
        this.gameObject.SetActive(false);
    }
   

    public void spawnBone(int eLevel)
    {
        Instantiate(prefab2Test);
    }
    */




}
